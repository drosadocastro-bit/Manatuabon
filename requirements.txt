aiohttp==3.13.5
astroquery==0.4.11
# dustmaps — optional: provides accurate per-sightline E(B-V) via SFD98 maps.
# Requires ~300 MB map data download (dustmaps.sfd.fetch()).  Without it the
# extinction_lookup module uses an analytical csc(|b|) fallback.
# dustmaps==1.0.14
langchain-anthropic==1.4.0
langchain-community==0.4.1
langchain-openai==1.1.12
numpy==2.4.4
PyYAML==6.0.3
python-dotenv==1.2.2
requests==2.33.1
sentence-transformers==5.3.0
watchdog==6.0.0
